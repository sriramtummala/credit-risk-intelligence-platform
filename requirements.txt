# Production dependencies — API server + training pipeline
# Used by: Docker, Render, any production deployment
# Install: pip install -r requirements.txt

# API
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.13.4

# ML inference + training
joblib==1.5.3
numpy==2.4.6
pandas==2.3.3
scikit-learn==1.9.0
xgboost==3.2.0

# Data ingestion (download + parse UCI XLS on first boot)
openpyxl==3.1.5
requests==2.34.2
